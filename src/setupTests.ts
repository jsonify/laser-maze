// src/setupTests.ts
import '@testing-library/jest-dom';

// This extends Vitest's expect with testing-library's jest-dom matchers
// Now you can use assertions like:
// - toBeInTheDocument()
// - toHaveTextContent()
// - toHaveStyle()
// and other DOM-specific matchers
